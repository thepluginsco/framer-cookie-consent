{
  "name": "@framer-cookie-consent/webflow-app",
  "version": "0.1.0",
  "private": true,
  "description": "Webflow App shell (Phase 3.2) — a Designer Extension that authors the banner and a Cloudflare Worker Data Client that holds OAuth and installs the loader via the shared engine.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "@framer-cookie-consent/shared": "*",
    "@framer-cookie-consent/shared-ui": "*",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@vitejs/plugin-react": "^6",
    "typescript": "^5.9",
    "vite": "^8"
  }
}
