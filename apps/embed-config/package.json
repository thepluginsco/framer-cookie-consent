{
  "name": "@framer-cookie-consent/embed-config",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted embed config page (Phase 3.1 App shell) — a static, zero-infra authoring UI for the universal <script> embed. Builds a banner config and copies the paste-ready snippet.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@framer-cookie-consent/shared": "*",
    "@framer-cookie-consent/shared-ui": "*",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@vitejs/plugin-react": "^6",
    "typescript": "^5.9",
    "vite": "^8"
  }
}
